[package]
name = "fastannoy"
version = "0.1.0"
description = "typos that are actually annoying"
license = "Apache-2.0"
edition = "2024"

[dependencies]
libc = "0.2"

[profile.release]
opt-level     = 3
lto           = "fat"
codegen-units = 1
panic         = "abort"
strip         = "symbols"
